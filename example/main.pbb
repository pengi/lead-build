{ cwd, ... }:
let
    compile_obj = pb.rule (
        {input, output, ...}:
        {
            command = ["gcc" "-c" "-o" output "-MMD" "-MF" "${output}.d" input];
            deps = "${output}.d";
        }
    );
in
(pb.build {
    output = cwd / "app";
    rule = compile_obj;
})